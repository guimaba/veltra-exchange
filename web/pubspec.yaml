name: blockchain_web
description: Interface web para a blockchain distribuida com mensageria RabbitMQ.
publish_to: none
version: 1.0.0

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  web_socket_channel: ^3.0.0
  provider: ^6.1.2
  intl: ^0.19.0
  url_launcher: ^6.3.0
  fl_chart: ^0.69.0

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
